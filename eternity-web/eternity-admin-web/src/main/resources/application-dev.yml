server:
  port: ${dev.admin.server.port}
spring:
  security:
    user:
      name: ${dev.admin.security.user.name}                               # security默认用户名
      password: ${dev.admin.security.user.password}                       # security默认密码
  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver                           # mysql数据库连接驱动
    url: ${dev.admin.datasource.url}                                      # mysql数据链接
    username: ${dev.admin.datasource.username}                            # mysql数据库账号
    password: ${dev.admin.datasource.password}                            # mysql数据库密码
  data:
    redis:
      host: ${dev.admin.redis.host}                                       #redis 链接
      port: ${dev.admin.redis.port}                                       #redis 端口
#      password: ${dev.admin.redis.password}                               #redis 密码
      database: 0                                                         #redis 数据库